name: "ReadTheDocs Trigger"
description: "Trigger a build on ReadTheDocs"
inputs:
  webhook_url:
    description: "The webhook URL from ReadTheDocs. This till have the form: https://readthedocs.org/api/v2/webhook/{webhook_id}/"
    required: true
  webhook_token:
    description: "The webhook token from ReadTheDocs"
    required: true
runs:
  using: "node12"
  main: "compiled/index.js"
